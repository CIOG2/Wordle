// import { Logica } from "./Logica.js";

const RenderLetrasLocalStorage = (palabras) => {
    // palabras.map((iteam) => {
    //     iteam.letras.forEach(element => {
    //         let evento = {
    //             target: {
    //                 classList: {
    //                     contains: ""
    //                 }
    //             },
    //             key: element,
    //         }
    //         // Logica(evento, 'TecladoFisico');
    //     });
    // })
}

export default RenderLetrasLocalStorage;